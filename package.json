{
  "name": "espresso-skins",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Decent DE1 skins sharing a headless core",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync-core": "node scripts/sync-core.mjs",
    "test": "node --test \"packages/core/test/*.test.mjs\"",
    "dev:mock": "npm run sync-core && node tests/mock-gateway/server.mjs"
  },
  "devDependencies": {
    "ws": "^8.21.0"
  }
}
